const Discord = require("discord.js");
const fs = require("fs");

module.exports = {
    interactionIndex: "template",
    disabled: false,
    run: async (client, config, interaction) => {
        
    }
}